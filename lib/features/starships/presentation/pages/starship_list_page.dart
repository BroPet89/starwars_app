import 'package:flutter/material.dart';

class StarshipListPage extends StatelessWidget {
  const StarshipListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Spaceships"),
      ),
    );
  }
}

